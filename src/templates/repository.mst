import { {{toEntityName tscName}} } from '../entities/{{toFileName tscName}}.entity';
import { dataSource } from "../../../core/datasource";

export const {{tscName}}Repository = dataSource.getRepository({{toEntityName tscName}})